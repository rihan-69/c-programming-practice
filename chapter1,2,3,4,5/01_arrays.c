#include <stdio.h>

int main() {
    int marks[90];
    marks[0] = 67;
    marks [1] = 78;
    //we can go all the way to marks [89] 
    printf("the marks at 0 and marks at 1 is %d %d\n",marks[0],marks[1]);   
    return 0;
}