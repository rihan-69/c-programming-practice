#include <math.h>
#include <stdio.h>

int main()
{
    int a;
    printf("pls enter the number you want square of\n");
    scanf("%d", &a);
    printf("the square of the number you have entered is %f\n", pow(a, 2));

        return 0;
}