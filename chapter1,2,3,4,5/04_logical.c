#include <stdio.h>

int main()
{
    int a = 1;
    int b = 0;
    printf("the value of a and b is %d\n", a && b);
    printf("the value of a or b is %d\n", a || b);
    printf("the value of not(a) id %d\n", !a);
    /*
    so not operator is basically a manipulator who galights the 
    variables to change their values*/
    /*
    so basically its like and requires both values to be true
    and or its like it requires one value to be true
    logical operators help us from the hassel of to many
    is satements
    */

    return 0;
}