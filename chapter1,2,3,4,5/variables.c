#include <stdio.h>

int main()
{
    int i = 10; // declare and initialize 'i' with 10
    int j = i;
    // %d is called format specifier
    // %d is for the int and %f is for the float and %c is for the char
    int a = 2, b = 3, c = 4, d = 5;
    printf("the value of i is %d\n and the value of the j is %d\n", i, j);
    printf("the value of i is %d\n and the value of the c is %d\n", i, c);
    printf("the value of i is %d\n and the value of the b is %d\n", i, b);

    return 0;
}
