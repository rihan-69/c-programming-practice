#include <stdio.h>
#include <string.h>
/*
 * this function is used to get the lenght of the string exculding the null character*/
int main() {
	char st[] = "rihan";
	printf("%d", strlen(st));
    
    return 0;
}
